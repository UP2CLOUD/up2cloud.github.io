{
  "pt": {
    "hero": "Terraform em Escala: Boas Práticas de IaC para <span class=\"gradient-text\">Times Multi-Cloud</span>",
    "body": "<p>O Terraform é o padrão de fato para infraestrutura em nuvem — mas equipes que não adotam práticas disciplinadas desde cedo pagam um preço alto depois: state drift, módulos espalhados, configurações impossíveis de testar, e o medo constante de um <code>terraform apply</code> dar errado em produção. Este guia destila os padrões que aplicamos em engajamentos multi-cloud na UP2CLOUD.</p><h2>Estrutura de Módulos: Pense em Bibliotecas, Não em Scripts</h2><p>O antipadrão mais comum que vemos é um repositório plano: um diretório, centenas de recursos, tudo emaranhado. Trate seu código Terraform como uma biblioteca de software com três camadas distintas:</p><ul class=\"reveal-list\"><li><strong>Módulos de recurso</strong> — wrappers reutilizáveis e de propósito único em torno de recursos do provider (ex.: <code>modules/gcs-bucket</code>, <code>modules/rds-instance</code>).</li><li><strong>Módulos de composição</strong> — montam módulos de recurso em unidades de infraestrutura com significado (ex.: <code>modules/web-service</code> que inclui um ALB, ASG e RDS).</li><li><strong>Configurações raiz</strong> — pontos de entrada específicos por ambiente, que chamam módulos de composição com variáveis específicas do ambiente.</li></ul><pre><code># Repository layout\ninfrastructure/\n  modules/\n    gcs-bucket/          # Resource module\n    rds-instance/        # Resource module\n    web-service/         # Composition module\n  environments/\n    prod/\n      main.tf            # Calls web-service module\n      variables.tf\n      terraform.tfvars\n    staging/\n      main.tf\n      variables.tf</code></pre><h2>Remote State: Nunca Use State Local em Times</h2><p>Arquivos de state local e controle de versão não combinam. Use <strong>GCS</strong> ou <strong>S3</strong> para remote state com locking habilitado via DynamoDB (AWS) ou locking nativo do GCS.</p><pre><code># GCS backend configuration\nterraform {\n  backend \"gcs\" {\n    bucket  = \"mycompany-tf-state-prod\"\n    prefix  = \"web-service/prod\"\n  }\n}\n\n# S3 + DynamoDB backend\nterraform {\n  backend \"s3\" {\n    bucket         = \"mycompany-tf-state\"\n    key            = \"web-service/prod/terraform.tfstate\"\n    region         = \"us-east-1\"\n    dynamodb_table = \"terraform-state-locks\"\n    encrypt        = true\n  }\n}</code></pre><p>Regra crítica: <strong>um arquivo de state por ambiente por serviço</strong>. Arquivos de state monolíticos que guardam centenas de recursos viram pesadelos de raio de explosão — um apply que falha pode travar toda a sua infraestrutura.</p><h2>Estratégia de Workspaces: Ambientes via Variáveis, Não Workspaces</h2><p>Workspaces do Terraform costumam ser mal usados para separação de ambientes. O problema: workspaces compartilham a configuração de backend e são fáceis de confundir. Recomendamos <strong>diretórios de configuração raiz separados por ambiente</strong> com uma biblioteca de módulos compartilhada. Use workspaces só para ambientes efêmeros de feature que espelham uma única configuração base.</p><h2>Policy as Code: Sentinel e OPA</h2><p>Governança sem aplicação é só documentação. Duas soluções maduras de policy-as-code se integram ao Terraform:</p><ul class=\"reveal-list\"><li><strong>HashiCorp Sentinel</strong> (Terraform Cloud/Enterprise) — roda políticas como um gate entre <code>plan</code> e <code>apply</code>. Excelente para organizações que já estão no stack HCP.</li><li><strong>OPA Conftest</strong> (open source) — valida o JSON do plan do Terraform contra políticas Rego em qualquer sistema de CI. Gratuito, flexível, agnóstico de nuvem.</li></ul><pre><code># OPA policy: deny resources without required tags\npackage terraform\n\ndeny[msg] {\n  resource := input.resource_changes[_]\n  resource.change.actions[_] == \"create\"\n  not resource.change.after.tags.env\n  msg := sprintf(\"Resource %v missing required 'env' tag\", [resource.address])\n}</code></pre><h2>Integração de CI/CD: GitHub Actions + Terraform Cloud</h2><p>Todo <code>terraform plan</code> deveria rodar automaticamente em pull requests; todo <code>terraform apply</code> deveria ser bloqueado até o merge na main e exigir aprovação. Aqui está um workflow de GitHub Actions mínimo, mas pronto para produção:</p><pre><code>name: Terraform Plan\non:\n  pull_request:\n    paths: ['infrastructure/**']\n\njobs:\n  plan:\n    runs-on: ubuntu-latest\n    steps:\n      - uses: actions/checkout@v4\n      - uses: hashicorp/setup-terraform@v3\n        with:\n          terraform_version: \"1.8.0\"\n          cli_config_credentials_token: ${{ secrets.TF_API_TOKEN }}\n      - name: Terraform Init\n        run: terraform init\n        working-directory: infrastructure/environments/prod\n      - name: Terraform Validate\n        run: terraform validate\n        working-directory: infrastructure/environments/prod\n      - name: OPA Policy Check\n        run: |\n          terraform show -json tfplan.binary | conftest test -\n        working-directory: infrastructure/environments/prod\n      - name: Post Plan to PR\n        uses: actions/github-script@v7\n        with:\n          script: |\n            github.rest.issues.createComment({...})</code></pre><h2>Evitando State Drift</h2><p>State drift acontece quando a infraestrutura real diverge do state do Terraform — geralmente porque alguém fez uma mudança pelo console. Três defesas:</p><ul class=\"reveal-list\"><li><strong>Execuções de detecção de drift</strong> — agende um <code>terraform plan</code> todas as noites e alerte sobre qualquer diff. O Terraform Cloud já tem isso embutido; para stacks open-source, use um workflow de GitHub Actions disparado por cron.</li><li><strong>Política de import-first</strong> — antes de mexer manualmente em qualquer recurso, importe-o para o state primeiro. Documente isso nos seus runbooks.</li><li><strong>Políticas de console somente leitura</strong> — restrinja os papéis IAM/GCP para que engenheiros não possam modificar recursos pelo console em ambientes de produção.</li></ul><h2>Dicas Práticas para Setups Multi-Cloud</h2><p>Ao gerenciar GCP e AWS a partir do mesmo repositório, use <strong>aliasing de provider</strong> e módulos claramente segmentados por namespace. Nunca misture módulos de recurso específicos de provider no mesmo diretório. Mantenha um <code>versions.tf</code> que fixa tanto a versão do Terraform quanto todas as versões de providers — upgrades de provider são a fonte mais comum de diffs de plan inesperados em codebases multi-cloud.</p><p>Terraform em escala recompensa o investimento em qualidade de módulos e ferramentas de governança. Times que tratam seu IaC como um codebase de software de produção — com reviews, testes, políticas e checks automatizados — gastam muito menos tempo apagando incêndios de drift e muito mais tempo entregando valor.</p><p class=\"mt-8 pt-8 border-t border-white/5 text-white/50 text-sm italic\">Quer implementar essas estratégias? Conheça nossos <a href=\"/#services\" class=\"text-sky-light hover:underline\">serviços de Cloud &amp; DevOps</a> ou saiba mais <a href=\"/about/\" class=\"text-sky-light hover:underline\">sobre a UP2CLOUD</a>.</p>"
  },
  "es": {
    "hero": "Terraform a Escala: Buenas Prácticas de IaC para <span class=\"gradient-text\">Equipos Multi-Cloud</span>",
    "body": "<p>Terraform es el estándar de facto para infraestructura cloud — pero los equipos que no adoptan prácticas disciplinadas desde el principio pagan un alto precio después: state drift, módulos dispersos, configuraciones imposibles de testear, y el temor constante a que un <code>terraform apply</code> salga mal en producción. Esta guía destila los patrones que aplicamos en proyectos multi-cloud en UP2CLOUD.</p><h2>Estructura de Módulos: Piensa en Librerías, No en Scripts</h2><p>El antipatrón más común que vemos es un repositorio plano: un directorio, cientos de recursos, todo enredado. Trata tu código Terraform como una librería de software con tres capas distintas:</p><ul class=\"reveal-list\"><li><strong>Módulos de recurso</strong> — wrappers reutilizables y de propósito único alrededor de recursos del provider (p. ej., <code>modules/gcs-bucket</code>, <code>modules/rds-instance</code>).</li><li><strong>Módulos de composición</strong> — ensamblan módulos de recurso en unidades de infraestructura con sentido (p. ej., <code>modules/web-service</code> que incluye un ALB, ASG y RDS).</li><li><strong>Configuraciones raíz</strong> — puntos de entrada específicos por entorno, que llaman a los módulos de composición con variables específicas del entorno.</li></ul><pre><code># Repository layout\ninfrastructure/\n  modules/\n    gcs-bucket/          # Resource module\n    rds-instance/        # Resource module\n    web-service/         # Composition module\n  environments/\n    prod/\n      main.tf            # Calls web-service module\n      variables.tf\n      terraform.tfvars\n    staging/\n      main.tf\n      variables.tf</code></pre><h2>Remote State: Nunca Uses State Local en Equipos</h2><p>Los archivos de state local y el control de versiones no combinan. Usa <strong>GCS</strong> o <strong>S3</strong> para remote state con locking habilitado vía DynamoDB (AWS) o locking nativo de GCS.</p><pre><code># GCS backend configuration\nterraform {\n  backend \"gcs\" {\n    bucket  = \"mycompany-tf-state-prod\"\n    prefix  = \"web-service/prod\"\n  }\n}\n\n# S3 + DynamoDB backend\nterraform {\n  backend \"s3\" {\n    bucket         = \"mycompany-tf-state\"\n    key            = \"web-service/prod/terraform.tfstate\"\n    region         = \"us-east-1\"\n    dynamodb_table = \"terraform-state-locks\"\n    encrypt        = true\n  }\n}</code></pre><p>Regla crítica: <strong>un archivo de state por entorno por servicio</strong>. Los archivos de state monolíticos que guardan cientos de recursos se convierten en pesadillas de radio de explosión — un apply fallido puede bloquear toda tu infraestructura.</p><h2>Estrategia de Workspaces: Entornos vía Variables, No Workspaces</h2><p>Los workspaces de Terraform suelen usarse mal para separar entornos. El problema: los workspaces comparten la configuración de backend y son fáciles de confundir. Recomendamos <strong>directorios de configuración raíz separados por entorno</strong> con una librería de módulos compartida. Usa workspaces solo para entornos efímeros de features que reflejan una única configuración base.</p><h2>Policy as Code: Sentinel y OPA</h2><p>La gobernanza sin aplicación es solo documentación. Dos soluciones maduras de policy-as-code se integran con Terraform:</p><ul class=\"reveal-list\"><li><strong>HashiCorp Sentinel</strong> (Terraform Cloud/Enterprise) — ejecuta políticas como una puerta entre <code>plan</code> y <code>apply</code>. Excelente para organizaciones que ya están en el stack HCP.</li><li><strong>OPA Conftest</strong> (open source) — valida el JSON del plan de Terraform contra políticas Rego en cualquier sistema de CI. Gratuito, flexible, agnóstico de nube.</li></ul><pre><code># OPA policy: deny resources without required tags\npackage terraform\n\ndeny[msg] {\n  resource := input.resource_changes[_]\n  resource.change.actions[_] == \"create\"\n  not resource.change.after.tags.env\n  msg := sprintf(\"Resource %v missing required 'env' tag\", [resource.address])\n}</code></pre><h2>Integración de CI/CD: GitHub Actions + Terraform Cloud</h2><p>Cada <code>terraform plan</code> debería ejecutarse automáticamente en los pull requests; cada <code>terraform apply</code> debería bloquearse hasta el merge a main y requerir aprobación. Aquí tienes un workflow de GitHub Actions mínimo pero listo para producción:</p><pre><code>name: Terraform Plan\non:\n  pull_request:\n    paths: ['infrastructure/**']\n\njobs:\n  plan:\n    runs-on: ubuntu-latest\n    steps:\n      - uses: actions/checkout@v4\n      - uses: hashicorp/setup-terraform@v3\n        with:\n          terraform_version: \"1.8.0\"\n          cli_config_credentials_token: ${{ secrets.TF_API_TOKEN }}\n      - name: Terraform Init\n        run: terraform init\n        working-directory: infrastructure/environments/prod\n      - name: Terraform Validate\n        run: terraform validate\n        working-directory: infrastructure/environments/prod\n      - name: OPA Policy Check\n        run: |\n          terraform show -json tfplan.binary | conftest test -\n        working-directory: infrastructure/environments/prod\n      - name: Post Plan to PR\n        uses: actions/github-script@v7\n        with:\n          script: |\n            github.rest.issues.createComment({...})</code></pre><h2>Evitando el State Drift</h2><p>El state drift ocurre cuando la infraestructura real diverge del state de Terraform — normalmente porque alguien hizo un cambio desde la consola. Tres defensas:</p><ul class=\"reveal-list\"><li><strong>Ejecuciones de detección de drift</strong> — programa un <code>terraform plan</code> cada noche y alerta ante cualquier diff. Terraform Cloud lo trae integrado; para stacks open-source, usa un workflow de GitHub Actions disparado por cron.</li><li><strong>Política de import-first</strong> — antes de tocar manualmente cualquier recurso, impórtalo al state primero. Documenta esto en tus runbooks.</li><li><strong>Políticas de consola de solo lectura</strong> — restringe los roles IAM/GCP para que los ingenieros no puedan modificar recursos desde la consola en entornos de producción.</li></ul><h2>Consejos Prácticos para Configuraciones Multi-Cloud</h2><p>Al gestionar GCP y AWS desde el mismo repositorio, usa <strong>aliasing de provider</strong> y módulos claramente segmentados por namespace. Nunca mezcles módulos de recurso específicos de un provider en el mismo directorio. Mantén un <code>versions.tf</code> que fije tanto la versión de Terraform como todas las versiones de providers — las actualizaciones de provider son la fuente más común de diffs de plan inesperados en codebases multi-cloud.</p><p>Terraform a escala recompensa la inversión en calidad de módulos y herramientas de gobernanza. Los equipos que tratan su IaC como un codebase de software de producción — con revisiones, tests, políticas y comprobaciones automatizadas — dedican mucho menos tiempo a apagar incendios de drift y mucho más a entregar valor.</p><p class=\"mt-8 pt-8 border-t border-white/5 text-white/50 text-sm italic\">¿Quiere implementar estas estrategias? Explore nuestros <a href=\"/#services\" class=\"text-sky-light hover:underline\">servicios de Cloud &amp; DevOps</a> o conozca más <a href=\"/about/\" class=\"text-sky-light hover:underline\">sobre UP2CLOUD</a>.</p>"
  },
  "fr": {
    "hero": "Terraform à Grande Échelle : Meilleures Pratiques IaC pour les <span class=\"gradient-text\">Équipes Multi-Cloud</span>",
    "stats": [],
    "body": "<p>Terraform est la norme de facto pour l’infrastructure cloud — mais les équipes qui n’adoptent pas de pratiques disciplinées dès le départ en paient le prix fort plus tard : dérive d’état (state drift), prolifération de modules, configurations impossibles à tester, et la peur constante qu’un <code>terraform apply</code> tourne mal en production. Ce guide distille les patterns que nous appliquons dans nos engagements multi-cloud chez UP2CLOUD.</p><h2>Structure des Modules : Pensez Bibliothèques, Pas Scripts</h2><p>L’anti-pattern le plus courant que nous observons est un dépôt plat : un seul répertoire, des centaines de ressources, tout emmêlé. Traitez votre code Terraform comme une bibliothèque logicielle avec trois couches distinctes :</p><ul class=\"reveal-list\"><li><strong>Modules de ressource</strong> — wrappers réutilisables et à usage unique autour des ressources du provider (ex. : <code>modules/gcs-bucket</code>, <code>modules/rds-instance</code>).</li><li><strong>Modules de composition</strong> — assemblent des modules de ressource en unités d’infrastructure cohérentes (ex. : <code>modules/web-service</code> qui inclut un ALB, un ASG et une RDS).</li><li><strong>Configurations racine</strong> — points d’entrée spécifiques à chaque environnement, qui appellent les modules de composition avec des variables propres à l’environnement.</li></ul><pre><code># Repository layout\ninfrastructure/\n  modules/\n    gcs-bucket/          # Resource module\n    rds-instance/        # Resource module\n    web-service/         # Composition module\n  environments/\n    prod/\n      main.tf            # Calls web-service module\n      variables.tf\n      terraform.tfvars\n    staging/\n      main.tf\n      variables.tf</code></pre><h2>Remote State : N’utilisez Jamais de State Local en Équipe</h2><p>Les fichiers de state local et le contrôle de version ne font pas bon ménage. Utilisez <strong>GCS</strong> ou <strong>S3</strong> pour le remote state, avec le locking d’état activé via DynamoDB (AWS) ou le locking natif de GCS.</p><pre><code># GCS backend configuration\nterraform {\n  backend \"gcs\" {\n    bucket  = \"mycompany-tf-state-prod\"\n    prefix  = \"web-service/prod\"\n  }\n}\n\n# S3 + DynamoDB backend\nterraform {\n  backend \"s3\" {\n    bucket         = \"mycompany-tf-state\"\n    key            = \"web-service/prod/terraform.tfstate\"\n    region         = \"us-east-1\"\n    dynamodb_table = \"terraform-state-locks\"\n    encrypt        = true\n  }\n}</code></pre><p>Règle critique : <strong>un fichier de state par environnement et par service</strong>. Les fichiers de state monolithiques qui contiennent des centaines de ressources deviennent des cauchemars en termes de rayon d’impact — un apply en échec peut verrouiller toute votre infrastructure.</p><h2>Stratégie de Workspaces : des Environnements via des Variables, Pas des Workspaces</h2><p>Les workspaces Terraform sont souvent mal utilisés pour séparer les environnements. Le problème : les workspaces partagent la configuration du backend et sont faciles à confondre. Nous recommandons des <strong>répertoires de configuration racine distincts par environnement</strong>, avec une bibliothèque de modules partagée. N’utilisez les workspaces que pour des environnements de feature éphémères qui reflètent une configuration de base unique.</p><h2>Policy as Code : Sentinel et OPA</h2><p>La gouvernance sans application n’est que de la documentation. Deux solutions matures de policy-as-code s’intègrent à Terraform :</p><ul class=\"reveal-list\"><li><strong>HashiCorp Sentinel</strong> (Terraform Cloud/Enterprise) — exécute des politiques comme un gate entre <code>plan</code> et <code>apply</code>. Excellent pour les organisations déjà sur la stack HCP.</li><li><strong>OPA Conftest</strong> (open source) — valide le JSON du plan Terraform par rapport à des politiques Rego, dans n’importe quel système de CI. Gratuit, flexible, agnostique au cloud.</li></ul><pre><code># OPA policy: deny resources without required tags\npackage terraform\n\ndeny[msg] {\n  resource := input.resource_changes[_]\n  resource.change.actions[_] == \"create\"\n  not resource.change.after.tags.env\n  msg := sprintf(\"Resource %v missing required 'env' tag\", [resource.address])\n}</code></pre><h2>Intégration CI/CD : GitHub Actions + Terraform Cloud</h2><p>Chaque <code>terraform plan</code> devrait s’exécuter automatiquement sur les pull requests ; chaque <code>terraform apply</code> devrait être bloqué jusqu’au merge sur main et nécessiter une approbation. Voici un workflow GitHub Actions minimal mais prêt pour la production :</p><pre><code>name: Terraform Plan\non:\n  pull_request:\n    paths: ['infrastructure/**']\n\njobs:\n  plan:\n    runs-on: ubuntu-latest\n    steps:\n      - uses: actions/checkout@v4\n      - uses: hashicorp/setup-terraform@v3\n        with:\n          terraform_version: \"1.8.0\"\n          cli_config_credentials_token: ${{ secrets.TF_API_TOKEN }}\n      - name: Terraform Init\n        run: terraform init\n        working-directory: infrastructure/environments/prod\n      - name: Terraform Validate\n        run: terraform validate\n        working-directory: infrastructure/environments/prod\n      - name: OPA Policy Check\n        run: |\n          terraform show -json tfplan.binary | conftest test -\n        working-directory: infrastructure/environments/prod\n      - name: Post Plan to PR\n        uses: actions/github-script@v7\n        with:\n          script: |\n            github.rest.issues.createComment({...})</code></pre><h2>Éviter le State Drift</h2><p>Le state drift survient lorsque l’infrastructure réelle diverge du state Terraform — généralement parce que quelqu’un a effectué un changement depuis la console. Trois défenses :</p><ul class=\"reveal-list\"><li><strong>Détection de drift planifiée</strong> — planifiez un <code>terraform plan</code> chaque nuit et alertez en cas de diff. Terraform Cloud propose cela nativement ; pour les stacks open-source, utilisez un workflow GitHub Actions déclenché par cron.</li><li><strong>Politique import-first</strong> — avant de toucher manuellement à une ressource, importez-la d’abord dans le state. Documentez cela dans vos runbooks.</li><li><strong>Politiques de console en lecture seule</strong> — restreignez les rôles IAM/GCP pour que les ingénieurs ne puissent pas modifier les ressources via la console dans les environnements de production.</li></ul><h2>Conseils Pratiques pour les Configurations Multi-Cloud</h2><p>Lorsque vous gérez GCP et AWS depuis le même dépôt, utilisez l’<strong>aliasing de provider</strong> et des modules clairement namespacés. Ne mélangez jamais des modules de ressource spécifiques à un provider dans le même répertoire. Conservez un <code>versions.tf</code> qui fixe à la fois la version de Terraform et toutes les versions de providers — les mises à jour de provider sont la source la plus courante de diffs de plan inattendus dans les codebases multi-cloud.</p><p>Terraform à grande échelle récompense l’investissement dans la qualité des modules et dans l’outillage de gouvernance. Les équipes qui traitent leur IaC comme un codebase logiciel de production — avec des reviews, des tests, des politiques et des contrôles automatisés — passent beaucoup moins de temps à éteindre des incendies de drift et beaucoup plus de temps à livrer de la valeur.</p><p class=\"mt-8 pt-8 border-t border-white/5 text-white/50 text-sm italic\">Vous souhaitez mettre en œuvre ces stratégies ? Découvrez nos <a href=\"/#services\" class=\"text-sky-light hover:underline\">services Cloud &amp; DevOps</a> ou en savoir plus <a href=\"/about/\" class=\"text-sky-light hover:underline\">sur UP2CLOUD</a>.</p>"
  }
}
